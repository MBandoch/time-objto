# OBJ_TO Sync Server — copie para .env e ajuste

# Porta de acesso do servidor (padrão: 3001)
PORT=3001

# Caminho do banco de dados SQLite (padrão: /data/tracker.db no container)
# DB_PATH=/data/tracker.db
